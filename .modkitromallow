# Exact-path exceptions for ModKit's coarse 8x8 perceptual-hash check.
# Every file remains subject to the unwaivable byte-identity and cache-path gates.
assets/fallback/pokemon_missing.png	Original VASC 16x16 question-mark fallback, hash-pinned from frozen vasc4j source; visually reviewed as independent placeholder art.
assets/runtime/dynamic_billboard_base.png	Original VASC transparent 16x16 GPU-allocation seed, hash-pinned from frozen vasc4j source; no game pixels are present.
assets/scenery/mini_trees.compact.png	Original ImageGen scenery atlas with retained master and both hashes recorded in ASSET_SOURCES.md.
assets/spawn_placeholder.png	Original VASC 16x16 green registration sentinel, hash-pinned from frozen vasc4j source; visually reviewed as independent placeholder art.
